{
  "name": "kvs-camera-viewer",
  "version": "1.0.0",
  "description": "Single-page app to view KVS WebRTC camera stream",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed for this SPA'",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  },
  "devDependencies": {
    "aws-cli": "^2.0.0"
  }
}
